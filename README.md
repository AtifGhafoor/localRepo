# LocalRepo

LocalRepo is a small demo static website included in the GitDemo workspace. It contains `index.html` and `style.css` and demonstrates a basic local development setup for static content.

To view the site, open `index.html` in a browser or serve the folder with a static file server.

